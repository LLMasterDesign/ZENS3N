[build]
builder = "railpack"

[deploy]
startCommand = "python3 satellites/3ox.studio/server.py --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
