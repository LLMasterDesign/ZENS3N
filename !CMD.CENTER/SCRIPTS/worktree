#!/usr/bin/env bash
# Quick wrapper for worktree-manager.rb

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
RUBY_SCRIPT="$SCRIPT_DIR/worktree-manager.rb"

if [[ ! -f "$RUBY_SCRIPT" ]]; then
  echo "✗ worktree-manager.rb not found" >&2
  exit 1
fi

ruby "$RUBY_SCRIPT" "$@"

